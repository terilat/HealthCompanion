services:
  health-companion:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - .:/app
    env_file:
      - .env
    command: bash -lc "pip install --no-cache-dir -r requirements.txt && python run_bot.py"
    restart: unless-stopped
    network_mode: host
